{
  "name": "@relay/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@relay/shared": "workspace:*",
    "convex": "^1.31.0",
    "zod": "^4.4.3"
  }
}
