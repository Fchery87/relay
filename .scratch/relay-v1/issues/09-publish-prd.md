# Publish the Relay v1 PRD

Type: task
Status: resolved
Blocked by: 01, 02, 03, 04, 05, 06, 07, 08, 10

## Question

Fold every resolved decision on this map into `docs/build-plan.md`, then run `/to-spec` to synthesize and publish the Relay v1 PRD to this tracker (`.scratch/relay-v1/PRD.md`) with `Status: ready-for-agent`. This ticket is the destination: when it closes, the way is clear and the build effort begins fresh.

## Answer

Done 2026-07-09. The PRD (published earlier at the founder's direction) now carries an Amendments section folding in all ten resolved decisions; `docs/build-plan.md` gained a "Resolved design decisions" section with the same gists; `tickets.md` updated where resolutions changed ticket wording (steering, checkpoints). The map's destination is reached — the build effort starts fresh from `tickets.md`, ticket "Walking skeleton: paired daemon shows up in the browser".
